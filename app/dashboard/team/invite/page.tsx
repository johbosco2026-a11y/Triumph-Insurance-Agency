'use client';
import { useState } from 'react';
export default function InvitePage(){const [email,setEmail]=useState(''),[role,setRole]=useState('MEMBER'),[result,setResult]=useState(''),[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setError('');setResult('');const r=await fetch('/api/team/invite',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,role})});const j=await r.json();if(!r.ok){setError(j.error);return}setResult(`Invitation created for ${j.email}. Accept URL: ${j.acceptUrl}`);setEmail('')}return <section className="dashboard-page narrow"><p className="eyebrow">Workspace</p><h1 className="page-title">Invite a team member.</h1><p className="section-intro">Invite agents and support staff into the Triumph workspace.</p><form className="card form-grid" onSubmit={submit}><div className="field full"><label>Email</label><input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field"><label>Workspace role</label><select value={role} onChange={e=>setRole(e.target.value)}><option value="MEMBER">Member</option><option value="VIEWER">Viewer</option></select></div><div className="field" style={{justifyContent:'end'}}><button className="btn btn-primary">Create invitation</button></div>{error&&<div className="notice error full">{error}</div>}{result&&<div className="notice success full">{result}</div>}</form></section>}
