import { headers } from 'next/headers'; import { redirect } from 'next/navigation'; import { auth } from '@/lib/auth'; import { prisma } from '@/lib/prisma';
export default async function SettingsPage(){const s=await auth.api.getSession({headers:await headers()});if(!s?.user)redirect('/login');const u=await prisma.user.findUnique({where:{id:s.user.id},include:{memberships:{include:{organization:true},take:1}}});return <section className="dashboard-page narrow"><p className="eyebrow">Account</p><h1 className="page-title">Settings</h1><p className="section-intro">Your account and workspace details.</p><div className="card settings-card"><h2>Profile</h2><div className="detail-grid"><div><small>Name</small><strong>{u?.name??'Not set'}</strong></div><div><small>Email</small><strong>{u?.email}</strong></div><div><small>Role</small><strong>{u?.role}</strong></div><div><small>Workspace</small><strong>{u?.memberships[0]?.organization.name??'None'}</strong></div></div></div></section>}
