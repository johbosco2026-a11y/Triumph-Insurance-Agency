'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { authClient } from '@/lib/auth-client';
export default function NewRequest(){const router=useRouter();const [pending,setPending]=useState(false);const [error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setPending(true);setError('');const data=Object.fromEntries(new FormData(e.currentTarget));try{const session=await authClient.getSession();if(!session.data?.user){router.push('/login?next=/dashboard/requests/new');return;}const res=await fetch('/api/requests',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const json=await res.json();if(!res.ok)throw new Error(json.error??'Unable to create request');router.push(`/dashboard/requests/${json.id}`);router.refresh()}catch(err){setError(err instanceof Error?err.message:'Something went wrong')}finally{setPending(false)}}return <section className="dashboard-page narrow"><p className="eyebrow">New request</p><h1 className="page-title">Start an insurance enquiry.</h1><p className="section-intro">Capture the essentials. Your team can continue the conversation from the request record.</p><form className="card form-grid" onSubmit={submit}><div className="field"><label htmlFor="request-name">Customer name</label><input id="request-name" name="name" required minLength={2}/></div><div className="field"><label htmlFor="request-email">Email</label><input id="request-email" name="email" type="email" required/></div><div className="field"><label htmlFor="request-phone">Phone</label><input id="request-phone" name="phone"/></div><div className="field"><label htmlFor="request-type">Insurance type</label><select id="request-type" name="type" defaultValue="Motor"><option>Motor</option><option>Home & Property</option><option>Health</option><option>Business</option><option>Travel</option><option>Life</option></select></div><div className="field full"><label htmlFor="request-message">What do you need?</label><textarea id="request-message" name="message" required minLength={2}/></div>{error&&<div className="notice error full">{error}</div>}<div className="full form-actions"><button className="btn btn-primary" disabled={pending}>{pending?'Saving…':'Create request'}</button><button type="button" className="btn btn-outline" onClick={()=>router.back()}>Cancel</button></div></form></section>}
