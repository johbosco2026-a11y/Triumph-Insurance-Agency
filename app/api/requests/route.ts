import { NextResponse } from 'next/server'; import { z } from 'zod'; import { headers } from 'next/headers'; import { auth } from '@/lib/auth'; import { prisma } from '@/lib/prisma';
const schema=z.object({name:z.string().min(2),email:z.string().email(),phone:z.string().optional(),type:z.string().min(1),message:z.string().min(2)});
export async function POST(req:Request){const s=await auth.api.getSession({headers:await headers()});if(!s?.user)return NextResponse.json({error:'Authentication required'},{status:401});const m=await prisma.membership.findFirst({where:{userId:s.user.id}});if(!m)return NextResponse.json({error:'No workspace is associated with this account'},{status:403});const parsed=schema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:'Please check the form fields'},{status:400});const r=await prisma.insuranceRequest.create({data:{...parsed.data,userId:s.user.id,organizationId:m.organizationId}});await prisma.auditLog.create({data:{userId:s.user.id,organizationId:m.organizationId,action:'REQUEST_CREATED',resource:r.id}});return NextResponse.json(r,{status:201});}
