'use client'; import { useState } from 'react'; import { useRouter } from 'next/navigation';
export default function QuotePage(){const [done,setDone]=useState(false);const [error,setError]=useState('');const [pending,setPending]=useState(false);const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setPending(true);setError('');const data=Object.fromEntries(new FormData(e.currentTarget));try{const r=await fetch('/api/quote',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json();if(!r.ok)throw new Error(j.error);setDone(true)}catch(e){setError(e instanceof Error?e.message:'Unable to send request')}finally{setPending(false)}}return <main className="page"><div className="container narrow"><p className="eyebrow">Get a quote</p><h1 className="page-title">Let’s find the right cover.</h1>{done?<div className="card"><h2>Request received.</h2><p>Thanks. A Triumph advisor can now follow up on your enquiry.</p><button className="btn btn-primary" onClick={()=>router.push('/')}>Back to home</button></div>:<form className="card form-grid" onSubmit={submit}><div className="field"><label>Name</label><input name="name" required/></div><div className="field"><label>Email</label><input name="email" type="email" required/></div><div className="field"><label>Phone</label><input name="phone"/></div><div className="field"><label>Insurance type</label><select name="type"><option>Motor</option><option>Home & Property</option><option>Health</option><option>Business</option><option>Life</option><option>Travel</option></select></div><div className="field full"><label>Tell us what you need</label><textarea name="message" required minLength={2}/></div>{error&&<div className="notice error full">{error}</div>}<button className="btn btn-gold full" disabled={pending}>{pending?'Sending…':'Request a quote'}</button></form>}</div></main>}
